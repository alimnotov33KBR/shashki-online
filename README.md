# Шашки v5.1.8 Online PRO — NA STUDIO Splash FIX

Исправлена только заставка поверх проверенной онлайн-версии.

## Что исправлено

- Заставка показывается ровно 3 секунды.
- На телефоне и компьютере изображение **всегда видно целиком**: корона, `NA STUDIO`, `PRESENTS`, `ШАШКИ` и нижняя часть макета не обрезаются.
- Чтобы на вытянутых экранах не было пустой чёрной области, за основной картинкой используется мягко затемнённый фон из той же заставки.
- Основное изображение использует `object-fit: contain`, фон — `cover`.
- Клик/касание по-прежнему пропускает заставку.
- Игровая логика, онлайн, reconnect, синхронизация и реванш не изменялись.
